# Для продакшена: полный URL API, если фронт на CDN/другом хосте
# В dev оставьте пустым — используется Vite proxy на /api
# VITE_API_BASE=https://iter.factiosi.com

# Куда проксировать /api в dev (по умолчанию в vite.config — 8010, если :8000 недоступен)
# VITE_API_PROXY=http://127.0.0.1:8010
